package controllers

import "volunteer-api/models"

var modelsWrapper models.Models
var volunteerModel = modelsWrapper.Volunteer
var candidateModel = modelsWrapper.Candidate
var recruitmentCampaignModel = modelsWrapper.RecruitmentCampaign
